{
  "name": "comprosody-reader",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build:client": "esbuild client/reader-entry.js --bundle --platform=browser --format=iife --outfile=public/vendor-reader.js && esbuild client/ingest-entry.js --bundle --platform=browser --format=iife --outfile=public/pdf-ingest.js && cp node_modules/pdfjs-dist/build/pdf.worker.min.mjs public/pdf.worker.min.mjs",
    "build": "npm run build:client",
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "epubjs": "^0.3.93",
    "express": "^5.1.0",
    "express-rate-limit": "^8.6.0",
    "helmet": "^8.1.0",
    "jszip": "^3.10.1",
    "pdfjs-dist": "^5.7.284",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "esbuild": "^0.25.6"
  },
  "overrides": {
    "epubjs": {
      "@xmldom/xmldom": "^0.9.10"
    }
  }
}
